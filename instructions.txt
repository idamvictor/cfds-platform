We need to fix a regression introduced by the nav redesign on the recently edited preference/account pages.

Problem:
On small screens, when the sidebar collapses/disappears, the hamburger/menu trigger is missing. This makes sidebar navigation inaccessible on mobile/tablet widths.

This must be fixed on all recently edited pages:
- src/pages/Preferences/accounts-page.tsx
- src/pages/Preferences/settings-page.tsx
- src/pages/Preferences/PersonalInformation.tsx

Strict rules:
1. Do NOT modify business logic.
2. Do NOT modify store logic.
3. Do NOT modify service/API logic.
4. Do NOT modify route definitions.
5. Do NOT alter page body content.
6. Only restore the mobile sidebar trigger UI and its existing behavior.
7. Reuse the exact existing mobile trigger/pattern already used on the Markets page or another existing page with the same sidebar system.
8. Do NOT invent a new navigation pattern if one already exists in the codebase.

What to do:
1. Inspect the Markets page (and any related sidebar/nav components) to identify exactly how the mobile hamburger/menu trigger is rendered and wired.
2. Compare that against:
   - accounts-page.tsx
   - settings-page.tsx
   - PersonalInformation.tsx
3. Restore the same mobile sidebar trigger behavior on all three pages.
4. Reuse existing state/handlers if they are already present.
5. If a trigger component already exists elsewhere, reuse it.
6. Keep desktop behavior unchanged.
7. Keep the icon-only desktop sidebar exactly as it is now.
8. Keep TickerBar exactly as-is unless a wrapper/layout adjustment is required purely to place the mobile trigger correctly.

Important constraint:
- This is a UI/accessibility/responsive navigation fix.
- Do not change what the links do.
- Do not change the routes.
- Do not change the main page content.
- Do not refactor unrelated code.

Before editing, summarize:
1. which files you will inspect
2. which files you will change
3. where the existing mobile trigger pattern is coming from
4. whether you can restore the trigger by reusing existing state/handlers without changing logic

Then apply the fix.